---
// Componente Hero Slider para diseñopaginas.cl
---
<!-- Hero Section -->
<section class="hero-section" style="background-color: #EEEEF0;">
   <!-- Divider -->
   <div class="divider"></div>

   <div class="container">
      <!-- Hero Content -->
      <div class="hero-content">
         <!-- Swiper Container -->
         <div class="swiper hero-swiper">
            <div class="swiper-wrapper">
               <!-- Slide 1 -->
               <div class="swiper-slide">
                  <div class="row g-5">
                     <div class="col-12 col-lg-7 hero-titles-container">
                        <h2 class="hero-subtitle">Diseño Web Empresas Chile</h2>
                        <h1 class="mb-4 wow fadeInUp hero-main-title"
                         data-wow-duration="1000ms" data-wow-delay="400ms">
                         Diseño de Páginas Web Chile
                        </h1>
                     </div>

                     <div class="col-12 col-lg-5">
                        <p class="mb-4 wow fadeInUp" data-wow-duration="1000ms" data-wow-delay="600ms">
                           Desarrollamos páginas web únicas con diseño creativo y tecnología de vanguardia para posicionar su empresa en los primeros lugares de google.
                        </p>
                        <a class="btn border-2 btn-outline-light wow fadeInUp" data-wow-duration="1000ms"
                           data-wow-delay="800ms" href="/servicios">Ver Servicios <i class="ti ti-arrow-up-right"></i>
                          </a>
                     </div>
                  </div>
               </div>

               <!-- Slide 2 -->
               <div class="swiper-slide">
                  <div class="row g-5">
                     <div class="col-12 col-lg-7 hero-titles-container">
                        <h2 class="mb-0 wow fadeInUp hero-main-title" data-wow-duration="1000ms" data-wow-delay="400ms">
                           Sitios Web de Calidad Profesional
                        </h2>
                        <h3 class="hero-subtitle">Diseño Web Empresas con Resultados</h3>
                     </div>

                     <div class="col-12 col-lg-5">
                        <p class="text-white wow fadeInUp" data-wow-duration="1000ms" data-wow-delay="600ms">
                           Su sitio web es la primera impresión de su empresa.
                           Nuestros diseños profesionales generan <strong>
                           confianza inmediata y conversiones reales</strong>.
                            Descubra la calidad de nuestro trabajo a través de proyectos exitosos en nuestro portafolio.
                        </p>
                        <a class="btn border-2 btn-outline-light wow fadeInUp" data-wow-duration="1000ms"
                           data-wow-delay="800ms" href="/portafolio">Ver portafolio <i class="ti ti-arrow-up-right"></i></a>
                     </div>
                  </div>
               </div>

               <!-- Slide 3 -->
               <div class="swiper-slide">
                  <div class="row g-5">
                     <div class="col-12 col-lg-7 hero-titles-container">
                        <h2 class="mb-0 wow fadeInUp hero-main-title" data-wow-duration="1000ms" data-wow-delay="400ms">
                           Su sitio web 2025 con IA
                        </h2>
                        <h3 class="hero-subtitle">Diseño Web con Inteligencia artificial</h3>
                     </div>

                     <div class="col-12 col-lg-5">
                        <p class=" wow fadeInUp" data-wow-duration="1000ms" data-wow-delay="600ms">
                           la IA nos ayuda a asegurar que tu sitio sea altamente funcional y fácil de navegar para tus usuarios. Si no tienes tiempo de describir al 100% tus servicios te ayudamos con las nuevas tecnologías de la IA especialistas en crear contenido 
                           sobre tu empresa y tendrás una web optima para tus clientes potenciales.
                        </p>
                        <a class="btn border-2 btn-outline-light wow fadeInUp" data-wow-duration="1000ms"
                           data-wow-delay="800ms" href="/contacto">Solicitar Presupuesto <i class="ti ti-arrow-up-right"></i></a>
                     </div>
                  </div>
               </div>
            </div>

            <!-- Swiper Pagination -->
            <div class="swiper-pagination hero-pagination"></div>
         </div>
      </div>
   </div>

</section>

<style>
/* Hero Titles Visual Order */
.hero-titles-container {
  display: flex;
  flex-direction: column;
}

.hero-main-title {
  order: 2;
  font-size: 60px !important;
  font-weight: 500;
  line-height: 1.1;
  color: #ffffff !important;
}

.hero-subtitle {
  order: 1;
  font-size: 28px;
  color: #011C42 !important;
  margin-bottom: 1rem;
  font-family: 'Inter', sans-serif;
}

@media (max-width: 768px) {
  .hero-main-title {
    font-size: 1.8rem;
  }
  
  .hero-subtitle {
    font-size: 1rem;
  }
}

/* Hero Swiper Styles */
.hero-swiper {
  overflow: hidden;
  min-height: 350px;
}

.hero-swiper .swiper-slide {
  display: none;
}

.hero-swiper .swiper-slide-active {
  display: block;
}

.hero-swiper .swiper-slide:first-child:not(.swiper-slide-active) {
  display: none;
}

/* Hero Pagination */
.hero-pagination {
  position: absolute;
  bottom: 30px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 12px;
  z-index: 10;
}

.hero-pagination .swiper-pagination-bullet {
  width: 12px;
  height: 12px;
  background: rgba(255, 255, 255, 0.5);
  border-radius: 50%;
  cursor: pointer;
  transition: all 0.3s ease;
}

.hero-pagination .swiper-pagination-bullet-active {
  background: #ffffff;
  transform: scale(1.2);
}
</style>

<script>
// Simple slider without Swiper dependency
document.addEventListener('DOMContentLoaded', function() {
  const slides = document.querySelectorAll('.swiper-slide');
  const paginationContainer = document.querySelector('.hero-pagination');
  let currentSlide = 0;
  let slideInterval;

  // Clear any existing bullets first
  if (paginationContainer) {
    paginationContainer.innerHTML = '';
    
    // Create exactly 3 bullets
    for (let i = 0; i < 3; i++) {
      const bullet = document.createElement('span');
      bullet.className = 'swiper-pagination-bullet';
      bullet.addEventListener('click', () => {
        currentSlide = i;
        showSlide(currentSlide);
        startSlider();
      });
      paginationContainer.appendChild(bullet);
    }
  }

  function showSlide(index) {
    const pagination = document.querySelectorAll('.swiper-pagination-bullet');
    slides.forEach(slide => slide.classList.remove('swiper-slide-active'));
    pagination.forEach(bullet => bullet.classList.remove('swiper-pagination-bullet-active'));
    
    if (slides[index]) {
      slides[index].classList.add('swiper-slide-active');
    }
    if (pagination[index]) {
      pagination[index].classList.add('swiper-pagination-bullet-active');
    }
  }

  function nextSlide() {
    if (slides.length === 0) return;
    currentSlide = (currentSlide + 1) % 3; // Force only 3 slides
    showSlide(currentSlide);
  }

  function startSlider() {
    if (slideInterval) clearInterval(slideInterval);
    slideInterval = setInterval(nextSlide, 5000);
  }

  // Show first slide immediately
  setTimeout(() => {
    if (slides.length > 0) {
      showSlide(0);
      if (slides.length > 1) {
        startSlider();
      }
    }
  }, 200);
});
</script>
